.DS_Store
Thumbs.db
desktop.ini
*.log

# Secrets and local env (never commit)
.env
.env.*
!.env.example

# Internal design dumps (do not publish)
Context/

# Vercel CLI local link state
.vercel

# Node (added when serverless deps like stripe are installed)
node_modules/
